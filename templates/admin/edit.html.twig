{% extends 'layout.html.twig' %}

    {% block title %}<h1>Edit the Article! ?</h1>{% endblock %}

        {% block body %}
            <div class="row">
                <div class="col-sm-12">
                    {{ form(articleForm) }}
                    {{ form_start(articleForm, {'action': path('create_article'), 'method': 'GET'}) }}
                </div>
            </div>
        {% endblock %}