{% extends 'layout.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            {{ form(form) }}
            {{ form_start(form, {'action': path('create_article'), 'method': 'GET'}) }}
        </div>
    </div>
{% endblock %}